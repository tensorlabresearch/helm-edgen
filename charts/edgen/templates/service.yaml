apiVersion: v1
kind: Service
metadata:
  name: {{ include "edgen.fullname" . }}
  labels:
    {{- include "edgen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edgen.selectorLabels" . | nindent 4 }}
